backend:
  name: git-gateway

publish_mode: editorial_workflow

media_folder: "images/uploads" 

collections:
  - name: "posts"
    label: "Post"
    folder: "_posts"
    filter:
      field: language
      value: en
    fields:
      - {label: "Language", name: "language"}